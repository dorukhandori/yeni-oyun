/// <reference types="vite/client" />

/** Injected by vite.config.ts `define` — package.json version + short git hash at build time. */
declare const __APP_VERSION__: string;
declare const __APP_COMMIT__: string;
