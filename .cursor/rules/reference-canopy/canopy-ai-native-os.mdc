---
description: AI-native beginner OS for Canopy — one playable slice, Play proof, no new-game brainstorm
alwaysApply: false
---

> **Referans (Canopy / game-project).** Bu kural yeni-oyun (Glowsprig) için alwaysApply=false; ürün kimliği `.cursor/rules/project.mdc`.

# Canopy AI-native OS

Canonical: `docs/ccgs/AI-NATIVE-BEGINNER.md`. Skill: `ai-native-beginner`. All 49 agents follow this; do not paste it into agent files.

Sahip never shipped a game. Product is already **Canopy**. Do not `/brainstorm` another game. Do not recommend a Unity course as the gate.

## Session contract

- One session = one playable slice. Done = sahip Play (`production/qa/`) or EditMode for pure logic — never “it compiles.”
- Label evidence: Play/tests ran vs inferred from code. Do not claim feel.
- Numbers in ScriptableObjects; runtime state not on design SOs. New Input System only.
- Cross-system: Question → Options → Decision → Draft → Approval.
- No commit unless sahip asks. No `.unity` / `.prefab` YAML. MCP scene writes are not the default.

```
// BAD — vibe slice
"I'll add a shop, HUD polish, and a new tent scene this turn."

// GOOD — one slice
"Next is PC-09 Create Play only. Site waits. May I add the play-case row?"
```

## Traps (refuse)

New game idea, Phaser, NPK slider, Grant/Rep sale, `Input.GetAxis`, DOTS MVP, Asset Store session-eater, eight UX screens before Play, restyle a parked canvas, Meshy a pot.

Screen / asset / folder rules unchanged: `screen-pipeline.md`, `asset-pipeline.md`, `CANOPY-PROJECT-MANIFEST.md`.

Beginner “how do I use AI?” → skill `ai-native-beginner`. “What do I do now?” → `help`.
